---
name: moment-plan-generator
display_name: 朋友圈文案可视化生成器
display_name_en: WeChat Moment Plan Generator
description: 将 Markdown 文案、聊天记录或已确认的朋友圈内容文档，可视化为可交互的 HTML 页面，支持多风格模板、自动日期跳转、一键复制和防折叠混淆。
description_zh: 朋友圈文案可视化工具，生成带防折叠、自动跳转、一键复制功能的 HTML 页面。
description_en: Transform WeChat moment scripts into interactive HTML pages with anti-fold, auto-scroll, and one-click copy features.
category: productivity
version: 1.0.0
author: jianwu
allowed_tools: Read,Write,Bash,Edit
disable-model-invocation: false
user-invocable: true
---

# 朋友圈文案可视化生成器（moment-plan-generator）

## 核心功能

本技能将用户提供的 Markdown 文案、聊天记录或朋友圈内容文档，转换为**可交互的 HTML 页面**。生成的 HTML 具备以下功能：

1. **多风格模板**：支持不同视觉风格，用户可根据品牌调性选择
2. **自动日期跳转**：根据当前日期自动定位到当天应发的文案
3. **一键复制 + 防折叠混淆**：复制时自动插入零宽字符，避免微信朋友圈折叠
4. **文案优化**：清理多余换行和句号，重新排版

## 使用场景

当用户需要：
- 把朋友圈文案变成可浏览的 HTML 页面
- 让团队成员能快速找到并复制当天该发的文案
- 避免朋友圈文案被微信折叠
- 需要不同视觉风格来匹配品牌调性

**触发词**：朋友圈文案、文案可视化、防折叠、moment、朋友圈 HTML、文案模板

## 工作流程

### 第 1 步：确认输入文件

询问用户文案来源：
- Markdown 文件（.md）
- 聊天记录（微信/飞书导出）
- 已确认的朋友圈内容文档
- 或直接粘贴文案内容

### 第 2 步：选择风格模板

向用户展示可用风格，并说明每种风格的含义和适用场景：

| 风格 | 适用场景 | 视觉特点 |
|------|---------|---------|
| **default（默认）** | 通用、温暖、有质感 | 暖金色调，柔和排版 |
| **traditional（中国传统文化风）** | 国学、文化、历史类内容 | 朱红/墨黑，书法字体，宣纸纹理 |
| **geek（极客科技风）** | 科技、互联网、产品类内容 | 深蓝/荧光绿，等宽字体，代码感 |
| **cyberpunk（赛博朋克风）** | 潮流、年轻、创意类内容 | 霓虹紫/品红，高对比，未来感 |
| **consulting（咨询公司风）** | 商务、专业、企业类内容 | 深蓝/灰色，简洁严谨，埃森哲风格 |

**选择后**：读取对应的 `templates/{style}.html` 模板。

### 第 3 步：处理文案内容

1. **解析文案**：提取每条文案的内容、日期、阶段标签、配图建议
2. **清理排版**：
   - 删除每句末尾多余的句号（如连续多个"。"）
   - 合并过度换行（超过 2 个换行符合并为 2 个）
   - 统一标点符号（全角/半角）
3. **日期映射**：根据文案中的日期信息，生成日期→内容锚点的映射

### 第 4 步：生成 HTML

使用选定的模板，将处理后的文案注入，生成完整的 HTML 文件。

**HTML 包含**：
- 所有文案内容（按阶段分组）
- 日期自动跳转 JS
- 一键复制按钮（每条文案）
- 防折叠混淆算法（内嵌 JS）
- 目录导航（快速跳转）
- 响应式样式（PC + 移动端）

### 第 5 步：输出和部署

生成的 HTML 文件：
- **本地使用**：直接双击打开，所有功能正常
- **部署到服务器**：上传 HTML 文件，分享给团队
- **WorkBuddy 兼容**：纯静态文件，无需后端

## 防折叠混淆说明

### 原理

微信朋友圈折叠的主要原因：
1. **字数阈值**：>100 字且>6 行 → 折叠成一行
2. **重复检测**：同一段文字多人转发或短时间重复发布
3. **营销识别**：含"特惠""限时"等营销词汇

本技能使用**零宽字符混淆**：
- 在文本中随机插入不可见的 Unicode 字符（`​`, `‌`, `‍`, `﻿`）
- 打破重复性检测，避免被判定为复制粘贴
- 人眼看起来完全一样，不影响阅读

### 混淆强度

- **轻度**：每 20 个字符插入一个零宽字符
- **中度**（默认）：每 10-15 个字符插入
- **重度**：每 5-8 个字符插入

**注意**：微信算法持续更新，无法保证 100% 防折叠。建议配合：
- 先手动输入几个字符，再粘贴替换
- 分多次粘贴，每次不超过 100 字
- 将核心内容放在图片上

## 可选：大模型文案优化

如果用户希望文案内容可以有变化（不要求与原文案一模一样），可以在生成 HTML 之前，调用大模型对文案进行优化：
- 保持原有情感和原意
- 调整句式、修辞，增加差异化
- 避免与其他团队成员的文案高度重复

**此功能默认关闭**，需要用户主动提出。

## 文件结构

```
moment-plan-generator/
├── SKILL.md                    # 本文件
├── references/
│   └── styles-guide.md         # 风格选择指南
├── scripts/
│   └── anti-fold.js            # 防折叠混淆算法（参考实现）
├── templates/
│   ├── default.html            # 默认风格模板
│   ├── traditional.html        # 中国传统文化风
│   ├── geek.html               # 极客科技风
│   ├── cyberpunk.html          # 赛博朋克风
│   └── consulting.html         # 咨询公司风
└── assets/
    └── fangzhedie.png          # 防折叠小程序二维码（可选）
```

## 示例

**用户输入**：
```
我有一批朋友圈文案，在 `.claude/明理之光发售全案/朋友圈文案.md` 里。帮我生成 HTML，用默认风格。
```

**技能执行**：
1. 读取 `.claude/明理之光发售全案/朋友圈文案.md`
2. 解析文案内容（29 条，分 5 个阶段）
3. 使用 `templates/default.html` 模板
4. 生成 `moment-plan.html`
5. 用户打开 HTML，自动定位到今天，点击复制即可

## 注意事项

- 生成的 HTML 是**自包含的**，所有 CSS 和 JS 内嵌，无需外部依赖
- 防折叠混淆在复制时执行，不影响页面显示
- 日期自动跳转基于用户本地系统时间
- 模板可以扩展，后续可添加更多风格
